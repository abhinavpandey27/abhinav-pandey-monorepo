{
  "name": "@portfolio/cms",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"TODO: configure Payload dev server\"",
    "build": "echo \"TODO: configure Payload build\"",
    "start": "echo \"TODO: configure Payload runtime\"",
    "lint": "eslint \"src\" --ext .js,.ts --max-warnings=0 --no-error-on-unmatched-pattern",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "format": "prettier --check \"**/*.{js,ts,json,md}\""
  },
  "dependencies": {},
  "devDependencies": {
    "@portfolio/config": "workspace:*"
  }
}
